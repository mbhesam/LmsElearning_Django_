image: docker/compose:latest 

services:
  - docker:dind

stages:
  - build

build_project:
  stage: build
  script:
  - docker-compose build 
  - docker login -u hesam -p hesam 193.141.126.240:5000
  - docker push app-image:latest 193.141.126.240:5000/app-image:$CI_PIPELINE_ID
هفت تیر بلوار کریم خان خیابان خرمدند شمالی پلاک 95 طبقه سوم 